# Express JS

## PNPM

```cmd
pnpm add -D supertest nodemon jest babel-jest @babel/preset-env @types/bcrypt @types/express @types/jest @types/multer @types/supertest @types/swagger-ui-express @types/uuid @types/multer
```

```cmd
pnpm add bcrypt cors dotenv express express-rate-limit jose swagger-ui-express winston multer
```

## NPM

```cmd
npm i -D supertest nodemon jest babel-jest @babel/preset-env @types/bcrypt @types/express @types/jest @types/multer @types/supertest @types/swagger-ui-express @types/uuid @types/multer
```

```cmd
npm i bcrypt cors dotenv express express-rate-limit jose swagger-ui-express winston multer
```
