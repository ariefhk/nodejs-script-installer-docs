# Express JS

## PNPM

```cmd
pnpm add -D supertest nodemon jest babel-jest @babel/preset-env @types/bcrypt @types/express @types/jest @types/multer @types/supertest @types/swagger-ui-express @types/uuid
```

```cmd
pnpm add bcrypt cors dotenv express express-rate-limit jose swagger-ui-express winston
```

## NPM

```cmd
npm i -D supertest nodemon jest babel-jest @babel/preset-env @types/bcrypt @types/express @types/jest @types/multer @types/supertest @types/swagger-ui-express @types/uuid
```

```cmd
npm i bcrypt cors dotenv express express-rate-limit jose swagger-ui-express winston
```
